{
  "name": "dedupe-csv",
  "version": "1.0.4",
  "description": "A deduper for duplicated entries on a CSV files",
  "author": "Alvaro Fernandez - Builtvisible",
  "license": "ISC",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "bin": {
    "dedupe-csv": "./lib/index.js"
  },
  "engines": {
    "node": ">14.0.0",
    "npm": ">6.14.0"
  },
  "scripts": {
    "test": "jest",
    "watch": "jest --watchAll",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json",
    "prepare": "yarn build",
    "prepublishOnly": "yarn test && yarn lint",
    "preversion": "yarn lint",
    "version": "git add -A src",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "prettier": "^2.4.1",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "chalk": "4.1.2",
    "csv-parser": "^3.0.0",
    "csv-writer": "^1.6.0",
    "strip-bom-stream": "^5.0.0",
    "type": "^2.7.2"
  },
  "files": [
    "lib/**/*"
  ]
}
